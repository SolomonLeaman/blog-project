<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="container mt-5">
        <div class="px-4 py-5 my-5 text-center">
            <h1 class="display-5 fw-bold text-body-emphasis">Blog Project</h1>
            <div class="col-lg-6 mx-auto">
              <p class="lead mb-4">A responsive blog created to practice skills with Javascript, Node, Express, EJS, and various other methodologies learned in the bootcamp.</p>
              <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a href="/articles/new" class="btn btn-primary">New Article</a>
              </div>
            </div>
          </div>

        
        <% if (locals.articles) { %>
            <% articles.forEach(article => { %>
                <div class="card mt-5 mb-5">
                <h4 class="card-header"><%=article.title%></h4>
                <div class="card-body">
                    <h5 class="card-subtitle font-bold"><%=article.desc%></h5>
                    <p class="card-text mt-2"><%=article.post%></p>
                    <a href="/articles/<%=article.title%>" class="btn btn-secondary">Read More</a>
                    <form action="/articles/delete/<%=article.title%>?_method=DELETE" class="d-inline" method="POST" >
                        <button class="btn btn-danger">Delete</button>
                    </form>
                    <p class="card-text mt-2"><%=article.date%> <%=article.time%></p>
                </div>
            </div>
            <% }) %>
        <% } else { %>
            <p class="text-center">Make Your First Post!</p>
        <% } %>
       
    </div>
    

    
</body>
</html>